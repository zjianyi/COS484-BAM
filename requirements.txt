# Python dependencies for the Falcon Mamba [CACHE] LoRA SFT experiments on a Lambda H100.
# Install order matters a little: install torch first (matching CUDA), then the rest.
#
#   pip install torch==2.4.1 --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements.txt
#
# mamba-ssm and causal-conv1d ship CUDA kernels and need a working nvcc + matching torch.
# On a fresh Lambda H100 image they install fine via pip.

torch==2.4.1
transformers>=4.45.0,<4.50.0
peft>=0.13.0
accelerate>=1.0.0
datasets>=3.0.0
trl>=0.11.0
mamba-ssm>=2.2.2
causal-conv1d>=1.4.0
python-dotenv>=1.0.0
numpy>=1.26.0
sentencepiece>=0.2.0
protobuf>=4.25.0
