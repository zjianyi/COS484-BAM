# Python dependencies for the Falcon Mamba [CACHE] LoRA SFT experiments on a Lambda H100.
#
# Install with:
#   pip install torch==2.7.0+cu128 --index-url https://download.pytorch.org/whl/cu128
#   pip install -r requirements.txt
#
# Pinning torch==2.7.0+cu128 ensures pip's build isolation pulls the right wheel
# when compiling mamba-ssm and causal-conv1d CUDA extensions.

--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.7.0+cu128
transformers>=5.0.0
peft>=0.13.0
accelerate>=1.0.0
datasets>=3.0.0
trl>=0.11.0
mamba-ssm>=2.2.2
causal-conv1d>=1.4.0
python-dotenv>=1.0.0
numpy>=1.26.0
sentencepiece>=0.2.0
protobuf>=4.25.0
